.env
.task

data/
node_modules/
GoogleDrive-Downloads/

# database dumps must never be tracked - they contain production secrets
# and PII (see docker/compose/ubuntuserver/sync-db-to-pc.ps1)
DB-Backups/
import-work/
docker/compose/**/db.sql
db.sql
*.sql.gz

.claude/worktrees/

*.exe
*.db*

.DS_Store

__debug_bin*
data.db*

# gotta ignore the literal filename of the backend build for agents because
# they don't use .exe file extension so it's tough to catch-all for binaries.
# these files are basically just the contents of ./cmd/
backend
clean
migrate
seed
